# テンクルの作業方針

ユーザーから明示された、このリポジトリで継続して適用する希望。

- 実装・修正の依頼は、必要な動作確認と `main` への push まで含む。毎回の push 確認は不要。
- その依頼で別のブランチ指定、公開保留、push 禁止が明示された場合は、その最新の指示を優先する。
- 作業開始前と push 前に最新のリモート状態を確認する。他の変更を消さず、force push は行わない。
- `index.html` は必要な箇所だけを編集し、既存の別モードや素材を不用意に変更しない。
- 機能変更では `APP_VER` を更新し、コミット名は `vXXX: 変更概要` にする。
- `APP_VER` を更新したら `version.txt` も同じ値（例 `v325`）に必ず書き換える（起動時の自動更新チェックがこのファイルを見る）。
- push 成功と配信完了を区別して報告する。失敗や未反映を成功と報告しない。

この記録はプロジェクトの引き継ぎ用。安全上の制限や承認機構を無効化するものではない。
